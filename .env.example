# Copy to .env and fill in your credentials.
# auth.py uses python-dotenv to load this automatically (load_dotenv() is called at module import).
ENPHASE_EMAIL=your@email.com
ENPHASE_PASSWORD=yourpassword
